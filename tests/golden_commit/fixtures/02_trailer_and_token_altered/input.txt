fix(payments): 결제 재시도 시 `PaymentRetryQueue.enqueue()` 순서가 꼬이는 버그를 수정함

기존에는 재시도 큐에 넣을 때 우선순위를 계산하지 않아 순서가 뒤섞였습니다.
`enqueue()` 호출 시점에 우선순위를 함께 계산하도록 수정함.

Closes #482
Co-authored-by: Jane Doe <jane@example.com>
